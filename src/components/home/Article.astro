---
import { formatDate } from '../../utils'
import Card from '../card/Card.astro'
import CardTitle from '../card/Title.astro'
import CardEyebrow from '../card/Eyebrow.astro'
import CardDescription from '../card/Desc.astro'
import CardCta from '../card/Cta.astro'

interface Article {
  title: string
  description: string
  date: Date
  id: string
}

export interface ArticleWithSlug extends Article {
  slug: string
}

type Props = { article: ArticleWithSlug }
const { article } = Astro.props
---

<Card as="article">
  <CardTitle href={`/articles/${article.slug}`}>
    {article.title}
  </CardTitle>
  <CardEyebrow as="time" dateTime={article.date.toISOString()} decorate>
    {formatDate(article.date)}
  </CardEyebrow>
  <CardDescription>{article.description}</CardDescription>
  <CardCta>Read article</CardCta>
</Card>
