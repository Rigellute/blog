---
import { ContainerOuter, ContainerInner } from './Container.tsx'

type Props = {
  class?: string
  style?: any
}

const { class: className, ...props } = Astro.props
---

<ContainerOuter className={className} {...props}>
  <ContainerInner><slot /></ContainerInner>
</ContainerOuter>
