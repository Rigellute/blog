---
import { ContainerInner as ContainerInnerReact } from "./Container.tsx";
type Props = {
  class?: string;
};
const { class: className, ...props } = Astro.props;
---

<ContainerInnerReact className={className} {...props}>
  <slot />
</ContainerInnerReact>
